{
    "builds": [{
        "src": "config/settings.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.11" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "config/settings.py"
        }
    ]
}